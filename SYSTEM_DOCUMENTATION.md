# 📋 الدليل التقني والتشغيلي الشامل — نظام حضور أمواج

> **الإصدار:** 1.0 — **تاريخ الإنشاء:** 7 مارس 2026  
> **الغرض:** مرجع نهائي لفرق الموارد البشرية والإدارة والتطوير  
> **المشروع:** Amwag Attendance System (Next.js + Supabase)

---

## جدول المحتويات

1. [البنية التحتية وقيود قاعدة البيانات](#1-البنية-التحتية-وقيود-قاعدة-البيانات)
2. [منطق الورديات والعمل الإضافي](#2-منطق-الورديات-والعمل-الإضافي)
3. [المهام المجدولة والأتمتة (Cron Jobs)](#3-المهام-المجدولة-والأتمتة-cron-jobs)
4. [مصفوفة الحالات الاستثنائية (Edge Cases)](#4-مصفوفة-الحالات-الاستثنائية-edge-cases)
5. [واجهة المستخدم وحالات العرض](#5-واجهة-المستخدم-وحالات-العرض)

---

## 1. البنية التحتية وقيود قاعدة البيانات

### 1.1 مخطط قاعدة البيانات (Database Schema)

النظام يعتمد على جدولين رئيسيين في Supabase (PostgreSQL):

#### جدول `profiles` — بيانات الموظفين

| العمود | النوع | الوصف |
|--------|------|-------|
| `id` | UUID (PK) | مرتبط بـ `auth.users` — يُنشأ تلقائياً عند التسجيل |
| `email` | TEXT (UNIQUE) | البريد الإلكتروني — فريد لكل موظف |
| `full_name` | TEXT | الاسم الكامل |
| `role` | TEXT | الدور: `admin` أو `employee` أو `accountant` |
| `branch` | TEXT | اسم الفرع (مثل: ملوي، الجيزه، رمسيس...) |
| `job_title` | TEXT | المسمى الوظيفي |
| `shift_start` | TIME | بداية الوردية (مثل: `09:00`) |
| `shift_end` | TIME | نهاية الوردية (مثل: `17:00`) |
| `off_day` | TEXT | يوم الإجازة الأسبوعية (مثل: `friday`) |
| `overtime_enabled` | BOOLEAN | هل يُسمح بالعمل الإضافي؟ (افتراضياً: `TRUE`) |

#### جدول `attendance` — سجلات الحضور

| العمود | النوع | الوصف |
|--------|------|-------|
| `id` | UUID (PK) | معرّف فريد لكل سجل |
| `user_id` | UUID (FK) | مرتبط بـ `profiles.id` |
| `date` | DATE | تاريخ يوم العمل |
| `check_in_time` | TIMESTAMPTZ | وقت تسجيل الحضور |
| `check_out_time` | TIMESTAMPTZ | وقت تسجيل الانصراف |
| `ip_address` | TEXT | عنوان IP عند تسجيل الحضور |
| `check_out_ip` | TEXT | عنوان IP عند تسجيل الانصراف |
| `check_in_location` | TEXT | اسم الفرع عند الحضور |
| `check_out_location` | TEXT | اسم الفرع عند الانصراف |
| `status` | TEXT | الحالة: `present` / `late` / `absent` / `missing_checkout` |
| `late_minutes` | INTEGER | عدد دقائق التأخير |
| `early_departure_minutes` | INTEGER | عدد دقائق المغادرة المبكرة |
| `overtime_minutes` | INTEGER | عدد دقائق العمل الإضافي (بحد أقصى 180 دقيقة) |

#### جدول `branch_allowed_ips` — عناوين IP المسموحة لكل فرع

| العمود | النوع | الوصف |
|--------|------|-------|
| `id` | UUID (PK) | معرّف فريد |
| `branch_name` | TEXT | اسم الفرع |
| `ip_network` | TEXT | أول 3 أجزاء من عنوان IPv4 (مثل: `"81.10.30"`) |
| `description` | TEXT | وصف اختياري |
| `is_active` | BOOLEAN | هل الشبكة مُفعّلة؟ |

---

### 1.2 كيف يمنع النظام تسجيل الحضور المزدوج (Double Check-in)

النظام يضمن عدم تسجيل الحضور مرتين في نفس اليوم عبر **طبقتين من الحماية:**

#### الطبقة الأولى: قيد فريد على مستوى قاعدة البيانات
```sql
UNIQUE(user_id, date)
```
هذا القيد يعني أنه **مستحيل تقنياً** أن يكون لموظف واحد سجلان حضور في نفس اليوم. أي محاولة لإدراج سجل مكرر ستُرفض من PostgreSQL مباشرة.

#### الطبقة الثانية: `ignoreDuplicates` في كود التطبيق
```typescript
await supabase.from('attendance').upsert(
    { user_id, date, check_in_time, ... },
    { onConflict: 'user_id,date', ignoreDuplicates: true }
);
```
عند استخدام `ignoreDuplicates: true`:
- إذا لم يكن هناك سجل سابق → يتم **إنشاء** سجل جديد.
- إذا كان هناك سجل سابق → يتم **تجاهل** الطلب بالكامل بدون خطأ.

> ✅ **النتيجة:** حتى لو ضغط الموظف على زر "تسجيل الحضور" 100 مرة، لن يُسجّل إلا المرة الأولى فقط.

---

### 1.3 منطق التحقق من عناوين IP (IP Whitelisting)

النظام يفرض أن يكون الموظف **متصلاً بشبكة WiFi الخاصة بالشركة** عند تسجيل الحضور أو الانصراف.

#### كيف يعمل؟

```
┌─────────────────────┐     ┌──────────────────┐     ┌────────────────────────┐
│  جهاز الموظف        │────▶│  Reverse Proxy   │────▶│  سيرفر التطبيق        │
│  IP: 81.10.30.124   │     │  (Vercel/Nginx)  │     │  يقرأ: x-real-ip      │
└─────────────────────┘     └──────────────────┘     └────────────────────────┘
```

**المنطق التنفيذي خطوة بخطوة:**

1. **استخراج عنوان IP الحقيقي:** يقرأ النظام header `x-real-ip` أولاً (يضعه reverse proxy مثل Vercel/Nginx). إذا لم يكن موجوداً، يقرأ أول عنوان في `x-forwarded-for`.

2. **استخراج شبكة IP:** يأخذ النظام أول 3 أجزاء من العنوان (network prefix):
   ```
   81.10.30.124  →  "81.10.30"
   ```

3. **مطابقة مع الفروع:** يبحث في جدول `branch_allowed_ips` عن شبكة مطابقة **ومُفعّلة** (`is_active = true`).

4. **القرار:**
   - ✅ إذا وُجدت مطابقة → يُسمح بالعملية ويُسجّل اسم الفرع.
   - ❌ إذا لم تُوجد مطابقة → يُرفض الطلب برسالة: `"يجب أن تكون متصلاً بشبكة الشركة"` (HTTP 403).

> ⚠️ **ملاحظة أمنية:** header `x-real-ip` يُضبط من قبل reverse proxy (Vercel) ولا يمكن للمستخدم العادي تزويره. أما `x-forwarded-for` فيؤخذ أول عنوان فقط (الذي يضيفه edge proxy).

---

## 2. منطق الورديات والعمل الإضافي

### 2.1 نافذة تسجيل الحضور (Check-in Window)

النظام يحدد **نافذة زمنية** يُسمح فيها بتسجيل الحضور:

```
نافذة الحضور = [بداية الوردية - ساعة واحدة] إلى [نهاية الوردية]
```

**أمثلة:**

| الوردية | نافذة الحضور المسموحة |
|---------|----------------------|
| 09:00 - 17:00 | 08:00 صباحاً — 05:00 مساءً |
| 22:00 - 06:00 | 09:00 مساءً — 06:00 صباحاً (عبر منتصف الليل) |
| 00:30 - 08:30 | 11:30 مساءً — 08:30 صباحاً |

**المنطق الخاص بالورديات الليلية:**

النظام يتعامل مع الورديات التي تعبر منتصف الليل عبر دالة `isWithinTimeWindow`:
- إذا كانت `نهاية الوردية ≥ بداية الوردية` → نافذة عادية في نفس اليوم.
- إذا كانت `نهاية الوردية < بداية الوردية` → نافذة ليلية: السماح إذا كان الوقت ≥ بداية النافذة **أو** < نهاية النافذة.

---

### 2.2 حساب التأخير (Lateness)

عند تسجيل الحضور:
- إذا وصل الموظف **قبل أو في وقت** بداية الوردية → الحالة: `present`.
- إذا وصل **بعد** بداية الوردية → الحالة: `late` مع حساب `late_minutes`.

**مثال:**
```
بداية الوردية: 09:00
وقت الحضور: 09:23
→ late_minutes = 23, status = 'late'
```

**الورديات الليلية:** يتم التعامل مع فارق التوقيت عبر منتصف الليل:
```
بداية الوردية: 23:00
وقت الحضور: 00:30 (اليوم التالي)
→ diff = 30 - 1380 = -1350 → -1350 + 1440 = 90
→ late_minutes = 90, status = 'late'
```

---

### 2.3 الحد الديناميكي لمدة الجلسة (Session Limit)

عند تسجيل الانصراف، يتحقق النظام من أن الموظف لم يتجاوز الحد الأقصى المسموح:

```
الحد الأقصى = Math.max(14, shift_duration_hours + 5)
```

| مدة الوردية | الحد الأقصى للجلسة | التفسير |
|------------|-------------------|---------|
| 8 ساعات | 14 ساعة | max(14, 8+5=13) = 14 |
| 9 ساعات | 14 ساعة | max(14, 9+5=14) = 14 |
| 10 ساعات | 15 ساعة | max(14, 10+5=15) = 15 |
| 12 ساعة | 17 ساعة | max(14, 12+5=17) = 17 |

**لماذا هذه المعادلة؟**
- **الحد الأدنى 14 ساعة:** يغطي وردية 8 ساعات + ساعة حضور مبكر + 3 ساعات عمل إضافي + 2 ساعات هامش أمان.
- **+5 ساعات:** للورديات الطويلة (10-12 ساعة) التي تحتاج هامشاً أكبر.

> ❌ إذا تجاوز الموظف هذا الحد → يُرفض تسجيل الانصراف برسالة: `"انتهت جلستك. لقد نسيت تسجيل الانصراف من الوردية السابقة"` ويحتاج تدخل HR.

---

### 2.4 حساب العمل الإضافي (Overtime)

- يُحسب العمل الإضافي فقط إذا كان `overtime_enabled = true` في ملف الموظف.
- **الحد الأقصى:** 180 دقيقة (3 ساعات).
- **المعادلة:**
  ```
  إذا الموظف غادر بعد نهاية الوردية:
    overtime_minutes = min(دقائق_بعد_نهاية_الوردية, 180)
  ```

**مثال:**
```
نهاية الوردية: 17:00
تسجيل الانصراف: 19:15
→ overtime_minutes = min(135, 180) = 135 دقيقة
```

---

### 2.5 حساب المغادرة المبكرة (Early Departure)

إذا سجّل الموظف الانصراف **قبل** نهاية الوردية:
```
early_departure_minutes = الفرق بالدقائق بين الوقت الحالي ونهاية الوردية
```

---

### 2.6 فترة السماح الإضافية (3 ساعات Leeway)

قبل أن يُصنّف النظام الموظف كـ `missing_checkout` (نسي تسجيل انصرافه):
- يمنح fترة سماح = **180 دقيقة (3 ساعات)** بعد نهاية الوردية.
- هذه الفترة تُطبّق فقط إذا كان `overtime_enabled = true`.
- السبب: إعطاء الموظف فرصة لتسجيل الانصراف بعد العمل الإضافي.

**الجدول الزمني لموظف ورديته 09:00-17:00:**

| الوقت | ماذا يحدث؟ |
|-------|-----------|
| 09:00 | بداية الوردية |
| 17:00 | نهاية الوردية الرسمية |
| 17:01 - 20:00 | فترة العمل الإضافي (حتى 3 ساعات) |
| 20:00 | انتهاء فترة السماح — النظام جاهز لتصنيفه كـ `missing_checkout` |
| 20:15 أو أقرب تشغيل لـ cron | يتم تغيير الحالة إلى `missing_checkout` |

---

### 2.7 استعادة الحالة من `missing_checkout` إلى `present`/`late`

إذا عاد الموظف وسجّل الانصراف **بعد** أن صُنّف كـ `missing_checkout` ولكن **ضمن حد الجلسة الديناميكي**:

```typescript
if (resolvedStatus === 'missing_checkout') {
    resolvedStatus = (late_minutes > 0) ? 'late' : 'present';
}
```

**المعنى:** النظام يعيد تقييم الحالة تلقائياً:
- إذا كان الموظف متأخراً أصلاً → يعود إلى `late`.
- إذا لم يكن متأخراً → يعود إلى `present`.

> ✅ هذا يعني أن الموظف يمكنه "إنقاذ" سجله بتسجيل الانصراف طالما لم يتجاوز الحد الأقصى للجلسة.

---

## 3. المهام المجدولة والأتمتة (Cron Jobs)

### 3.1 دالة `mark_absent_employees()`

دالة PL/pgSQL تعمل على مستوى قاعدة البيانات وتُنفّذ المهام التالية:

```
┌──────────────────────────┐
│   pg_cron: كل 15 دقيقة  │
│   '*/15 * * * *'         │
└──────────┬───────────────┘
           ▼
┌──────────────────────────────────────────────────┐
│         mark_absent_employees()                  │
│                                                  │
│  1. حساب توقيت القاهرة (Africa/Cairo + DST)     │
│  2. جلب الموظفين (باستثناء من في إجازتهم)       │
│  3. لكل موظف:                                   │
│     ├─ هل الوردية انتهت؟ (+ فترة السماح 180 د)  │
│     │   ├─ لا → تخطي (skipped_shift++)          │
│     │   └─ نعم ↓                                 │
│     ├─ هل يوجد سجل حضور للتاريخ المستهدف؟       │
│     │   ├─ لا → إدراج سجل status='absent'       │
│     │   ├─ حضر ولم ينصرف → status='missing_checkout'│
│     │   └─ حضر وانصرف → تخطي (already_recorded++) │
│  4. إرجاع ملخص JSON                             │
└──────────────────────────────────────────────────┘
```

### 3.2 الحماية من التنفيذ المتكرر (Advisory Lock)

```sql
IF NOT pg_try_advisory_lock(hashtext('mark_absent_employees')) THEN
    RETURN '{"skipped": true}';
END IF;
```

- تستخدم الدالة `pg_try_advisory_lock` لمنع تشغيل نسختين في نفس الوقت.
- إذا كانت نسخة أخرى تعمل بالفعل → تُرجع `skipped: true` بدون أي تعديل.
- يُفرج عن القفل تلقائياً في نهاية الدالة.

### 3.3 مصفوفة التوقيت: متى تعمل الدالة وكيف تحسب؟

الدالة تعمل كل 15 دقيقة (`*/15 * * * *`):
```
00:00, 00:15, 00:30, 00:45, 01:00, ..., 23:45
```

#### حساب فترة السماح (`grace_minutes = 180`) عبر منتصف الليل

**وردية نهارية عادية (09:00 - 17:00):**
```
shift_end_min = 17*60 = 1020
grace_minutes = 180
الوردية تنتهي فعلياً عند: 1020 + 180 = 1200 دقيقة = 20:00
→ أول تشغيل cron بعد 20:00 هو 20:00 أو 20:15
→ الموظف يُصنّف كـ absent أو missing_checkout
```

**وردية ليلية (22:00 - 06:00):**
```
shift_start_min = 22*60 = 1320
shift_end_min = 6*60 = 360
grace_minutes = 180

الشرط: shift_end < shift_start → وردية ليلية
الوردية تنتهي فعلياً عند:
  shift_ended = (cairo_total_min >= 360+180 AND cairo_total_min < 1320)
             = (cairo_total_min >= 540 AND cairo_total_min < 1320)
             = (الوقت >= 09:00 صباحاً AND الوقت < 10:00 مساءً)

→ أقرب تشغيل cron: 09:00 صباحاً
→ target_date = أمس (لأن الوردية بدأت أمس)
```

**وردية تنتهي مع فترة سماح تعبر منتصف الليل (21:00 - 23:00):**
```
shift_end_min = 23*60 = 1380
grace_minutes = 180
1380 + 180 = 1560
لكن 1560 > 1440 (24 ساعة)

في هذه الحالة، shift_ended = (cairo_total_min >= 1560) 
→ لكن cairo_total_min لا يتجاوز 1439
→ الدالة لن تجد أن الوردية انتهت إلا في اليوم التالي عندما cairo_total_min >= 120 (02:00 صباحاً)
ملاحظة: هذا السيناريو نادر وسيُعالج في التشغيل التالي
```

### 3.4 التاريخ المستهدف (target_date)

- **ورديات نهارية:** التاريخ المستهدف = اليوم.
- **ورديات ليلية:** إذا كان الوقت الحالي صباحاً (أقل من بداية الوردية) → التاريخ المستهدف = **أمس**.

```sql
IF shift_end_min < shift_start_min AND cairo_total_min < shift_start_min THEN
    target_date := cairo_date - INTERVAL '1 day';
ELSE
    target_date := cairo_date;
END IF;
```

### 3.5 طرق التشغيل

| الطريقة | التفاصيل |
|---------|---------|
| **تلقائي** | pg_cron كل 15 دقيقة (`*/15 * * * *`) |
| **يدوي** | زر "تسجيل الغياب" في لوحة المشرف → `POST /api/attendance/mark-absent` |
| **مباشر** | `SELECT mark_absent_employees();` في Supabase SQL Editor |

---

## 4. مصفوفة الحالات الاستثنائية (Edge Cases)

### 4.1 ❓ ماذا لو حاول الموظف استخدام VPN لتسجيل الانصراف؟

**الإجابة: سيُرفض.**

| الخطوة | التفاصيل |
|--------|---------|
| 1 | الموظف يتصل بـ VPN → عنوان IP يتغير إلى عنوان خادم VPN (مثل: `185.72.xxx.xxx`) |
| 2 | Reverse proxy (Vercel) يُسجّل عنوان IP الحقيقي للاتصال في `x-real-ip` |
| 3 | النظام يستخرج الشبكة: `"185.72.xxx"` |
| 4 | لا يجد مطابقة في `branch_allowed_ips` |
| 5 | **يُرفض الطلب: HTTP 403** — `"يجب أن تكون متصلاً بشبكة الشركة"` |

> 🔒 حتى لو حاول الموظف إضافة header `x-forwarded-for` مزيف، النظام يعتمد أولاً على `x-real-ip` الذي يضعه Vercel مباشرة ولا يمكن تزويره من المتصفح.

---

### 4.2 ❓ ماذا لو انقطع الإنترنت الرئيسي وتحولت الشبكة إلى IP احتياطي؟

**الإجابة: يجب إضافة الـ IP الجديد إلى جدول `branch_allowed_ips`.**

| السيناريو | النتيجة |
|-----------|--------|
| الإنترنت الرئيسي يعمل (IP مسجّل) | ✅ تسجيل الحضور يعمل بشكل طبيعي |
| انقطاع وتحول لـ IP احتياطي (غير مسجّل) | ❌ تسجيل الحضور يُرفض |
| المشرف يُضيف IP الاحتياطي إلى الجدول | ✅ يعود للعمل فوراً |

**الإجراء المطلوب من الإدارة:**
1. تحديد عنوان IP الاحتياطي (أول 3 أجزاء).
2. إضافته إلى جدول `branch_allowed_ips` عبر Supabase Dashboard.
3. يمكن تعطيله لاحقاً عبر تعيين `is_active = false`.

> 💡 **نصيحة:** يُنصح بإضافة عناوين IP الاحتياطية مسبقاً لكل فرع لتجنب أي تعطل.

---

### 4.3 ❓ ماذا لو بقي موظف بوردية 12 ساعة لمدة 14.5 ساعة؟

**الإجابة: يمكنه تسجيل الانصراف بنجاح.**

```
مدة الوردية: 12 ساعة
الحد الأقصى = max(14, 12 + 5) = 17 ساعة
14.5 ساعة < 17 ساعة → ✅ مسموح
```

**التفاصيل:**
- **العمل الإضافي المُسجّل:** 14.5 - 12 = 2.5 ساعة = 150 دقيقة (أقل من الحد 180)
- **الحالة:** `present` أو `late` (حسب وقت الحضور)

| مدة البقاء | النتيجة | العمل الإضافي |
|-----------|--------|-------------|
| 12 ساعة (في الموعد) | ✅ انصراف عادي | 0 دقيقة |
| 14 ساعة | ✅ مسموح | 120 دقيقة (ساعتان) |
| 14.5 ساعة | ✅ مسموح | 150 دقيقة |
| 15 ساعة | ✅ مسموح | 180 دقيقة (الحد الأقصى) |
| 15.5 ساعة | ✅ مسموح | 180 دقيقة (مُقيّد) |
| 17 ساعة | ✅ آخر لحظة مسموحة | 180 دقيقة |
| 17 ساعة + 1 دقيقة | ❌ مرفوض — جلسة منتهية | — |

---

### 4.4 ❓ ماذا لو نسي الموظف تسجيل الانصراف وعاد في اليوم التالي؟

**النتيجة تعتمد على الوقت المنقضي:**

#### السيناريو أ: العودة ضمن حد الجلسة
```
وردية: 09:00-17:00 (8 ساعات)
حد الجلسة: 14 ساعة
حضور: 08:45 صباحاً
→ آخر لحظة للانصراف: 08:45 + 14 = 10:45 مساءً

إذا عاد وسجّل الانصراف في 10:00 مساءً → ✅ يُقبل
→ الحالة تعود من missing_checkout إلى present/late
```

#### السيناريو ب: العودة بعد حد الجلسة (الغالب)
```
حضور: 08:45 صباحاً
→ آخر لحظة للانصراف: 10:45 مساءً

إذا حاول تسجيل الانصراف في اليوم التالي الساعة 09:00 صباحاً:
→ hoursSinceCheckIn = ~24.25 ساعة > 14 ساعة
→ ❌ HTTP 400: "انتهت جلستك. لقد نسيت تسجيل الانصراف"
```

**الإجراء المطلوب من HR:**
1. فتح Supabase Dashboard.
2. العثور على سجل الحضور المعني في جدول `attendance`.
3. تعديل الحقول يدوياً:
   - إضافة `check_out_time`.
   - تعديل `status` من `missing_checkout` إلى `present` أو `late`.
   - حساب وإضافة `overtime_minutes` و `early_departure_minutes` يدوياً.

---

### 4.5 ❓ ماذا لو لم يحضر الموظف طوال اليوم؟

**الإجابة: يمر بمرحلتين:**

#### المرحلة 1: أثناء اليوم — حالة `pending` (افتراضية في الواجهة)

| ما يراه المشرف | ما يوجد في القاعدة |
|---------------|-------------------|
| ⏳ شارة "في الانتظار" متحركة (animate-pulse) | **لا يوجد سجل** — هذه حالة افتراضية (virtual) |
| معرّف السجل يبدأ بـ `virtual-` | لا يوجد `id` حقيقي في جدول `attendance` |
| لا يوجد وقت حضور أو انصراف | — |

**كيف يعمل هذا تقنياً (Frontend Virtualization):**
1. الـ API يجلب جميع الموظفين من `profiles` (باستثناء من في إجازتهم).
2. يجلب جميع سجلات الحضور لليوم.
3. يُنشئ سجلات "وهمية" للموظفين الذين ليس لهم سجل حضور.
4. هذه السجلات تظهر بحالة `pending` بشارة متحركة.

#### المرحلة 2: نهاية اليوم — حالة `absent` (رسمية في القاعدة)

عندما يعمل الـ cron بعد انتهاء الوردية + فترة السماح:
1. يجد أن الموظف ليس لديه سجل حضور.
2. يُدرج سجلاً رسمياً: `status = 'absent'`.
3. تتحول الشارة في الواجهة من ⏳ `pending` إلى 🔴 `absent`.

---

### 4.6 ❓ ماذا لو حاول الموظف تسجيل الحضور في وقت متأخر من الليل يعبر منتصف الليل؟

**الإجابة: يعتمد على وردية الموظف.**

#### السيناريو أ: موظف ورديته 22:00 - 06:00
```
محاولة تسجيل الحضور: 23:30
نافذة الحضور: 21:00 - 06:00
23:30 ∈ [21:00, 06:00) → ✅ يُسمح
التاريخ المسجّل: تاريخ اليوم (حسب توقيت القاهرة عند 23:30)
```

#### السيناريو ب: موظف ورديته نهارية (09:00 - 17:00)
```
محاولة تسجيل الحضور: 23:30
نافذة الحضور: 08:00 - 17:00
23:30 ∉ [08:00, 17:00) → ❌ مرفوض
رسالة: "تسجيل الحضور مسموح فقط بين 08:00 AM و 05:00 PM"
```

#### السيناريو ج: موظف ورديته 23:00 - 07:00 ويسجّل الحضور 00:15
```
نافذة الحضور: 22:00 - 07:00
بداية الوردية: 23:00
00:15 ∈ [22:00, 07:00) → ✅ يُسمح

حساب التأخير:
diff = 15 (00:15) - 1380 (23:00) = -1365
-1365 < -720 → diff += 1440 → diff = 75
→ late_minutes = 75, status = 'late'
```

---

### 4.7 ❓ ماذا لو سجّل الموظف الحضور من فرع والانصراف من فرع آخر؟

**الإجابة: مسموح — يُسجّل كلا الموقعين بشكل منفصل.**

```
check_in_location = "ملوي"        (من IP فرع ملوي)
check_out_location = "الجيزه"     (من IP فرع الجيزه)
```

كل عملية (حضور/انصراف) تتحقق بشكل مستقل من عنوان IP وتسجّل اسم الفرع الخاص بها. لا يُشترط تطابق الفرعين.

---

### 4.8 ❓ ماذا لو حاول شخصان تسجيل الغياب في نفس الوقت (Race Condition)؟

**الإجابة: محمي عبر 3 طبقات.**

| الطبقة | الآلية |
|--------|-------|
| 1️⃣ Advisory Lock | `pg_try_advisory_lock` يمنع تشغيل نسختين من الدالة في نفس الوقت |
| 2️⃣ ON CONFLICT DO NOTHING | حتى لو مرّ القفل، إدراج سجل `absent` مكرر سيُتجاهل |
| 3️⃣ UNIQUE(user_id, date) | قيد فريد على مستوى قاعدة البيانات يمنع أي تكرار |

---

### 4.9 ❓ ماذا لو كان اليوم هو يوم إجازة الموظف؟

**الإجابة: يتم تجاهل الموظف تماماً.**

```sql
WHERE role = 'employee'
  AND (off_day IS NULL OR off_day <> cairo_day_name)
```

- لا يظهر في قائمة `pending` في الواجهة.
- لا يُسجّل كـ `absent` من قبل الـ cron.
- لا يستطيع تسجيل الحضور (لا يوجد قيد تقني لكنه خارج التقارير).

---

## 5. واجهة المستخدم وحالات العرض

### 5.1 السجلات الافتراضية (Virtual Records) vs السجلات الحقيقية

| الخاصية | سجل حقيقي (DB Record) | سجل افتراضي (Virtual Record) |
|---------|---------------------|---------------------------|
| **المعرّف** | UUID عشوائي (مثل: `a3f2...`) | `virtual-{userId}` |
| **مصدره** | جدول `attendance` في قاعدة البيانات | يُنشأ في الـ API عند طلب البيانات |
| **متى يتحول لحقيقي؟** | عند تسجيل الحضور أو عند تشغيل cron الغياب |
| **هل يمكن تعديله؟** | نعم | لا — هو للعرض فقط |
| **الحالة** | `present` / `late` / `absent` / `missing_checkout` | `pending` دائماً |

### 5.2 شارات الحالة (Status Badges)

| الحالة | اللون | الوصف | متى تظهر؟ |
|--------|------|------|-----------|
| 🟢 `present` | أخضر زمردي | حاضر | الموظف سجّل الحضور في الوقت |
| 🟡 `late` | أصفر كهرماني | متأخر | الموظف سجّل الحضور بعد بداية الوردية |
| 🔴 `absent` | أحمر | غائب | سجل رسمي — الموظف لم يحضر (بعد تشغيل cron) |
| 🟠 `missing_checkout` | برتقالي | لم يسجّل انصراف | الموظف حضر لكن لم يسجّل الانصراف (بعد cron) |
| ⏳ `pending` | كهرماني + **نبض متحرك** (`animate-pulse`) | في الانتظار | سجل افتراضي — الموظف لم يسجّل الحضور بعد (أثناء اليوم) |

### 5.3 الفرق بين `pending` و `absent`

```
┌─────────────┐    تسجيل حضور    ┌─────────────┐
│   pending   │ ──────────────▶  │  present    │
│  (افتراضي)  │                  │  أو late    │
└─────┬───────┘                  └─────────────┘
      │
      │ cron بعد نهاية
      │ الوردية + 3 ساعات
      ▼
┌─────────────┐
│   absent    │
│  (رسمي DB)  │
└─────────────┘
```

| | `pending` | `absent` |
|--|---------|---------|
| **المصدر** | واجهة المستخدم (Frontend) | قاعدة البيانات (Backend/Cron) |
| **التوقيت** | أثناء يوم العمل | بعد انتهاء الوردية + فترة السماح |
| **الأهمية** | إشعار مبدئي للمشرف | سجل رسمي يُحسب في التقارير |
| **المعرّف** | `virtual-XXXX` | UUID حقيقي |
| **قابل للتعديل** | لا | نعم (بواسطة HR في Supabase) |

---

### 5.4 الفروع المسجلة في النظام

| الفرع |
|-------|
| ملوي |
| الأضافيه |
| شلبي |
| بني مزار |
| الجيزه |
| رمسيس |
| محرم بك |
| شرم الشيخ |
| الغردقه |
| IT Department |

---

## ملخص سريع للقواعد الحاكمة

| القاعدة | القيمة |
|---------|--------|
| حد تسجيل الحضور المسبق | ساعة واحدة قبل بداية الوردية |
| حد العمل الإضافي | 180 دقيقة (3 ساعات) |
| فترة السماح قبل تسجيل الغياب | 180 دقيقة (3 ساعات) بعد نهاية الوردية |
| حد الجلسة | `max(14, مدة_الوردية + 5)` ساعة |
| فترة تشغيل cron | كل 15 دقيقة |
| المنطقة الزمنية | `Africa/Cairo` (تدعم التوقيت الصيفي DST) |
| حماية التسجيل المزدوج | `UNIQUE(user_id, date)` + `ignoreDuplicates` |
| حماية التنفيذ المتكرر للـ cron | `pg_try_advisory_lock` |
| التحقق من الشبكة | أول 3 أجزاء من IPv4 مقابل `branch_allowed_ips` |

---

> 📝 **آخر تحديث:** 7 مارس 2026  
> 🔧 **المطوّر:** نظام حضور أمواج — Amwag Attendance System v2
